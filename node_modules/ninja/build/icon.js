'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Icon = Icon;

var _react = require('react');

var _pause = require('./graphics/pause');

var _play = require('./graphics/play');

var _stop = require('./graphics/stop');

var graphics = {
  pause: _pause.Pause,
  play: _play.Play,
  stop: _stop.Stop
};

function Icon(props) {
  var graphic = props.graphic;

  var style = Object.assign({}, Icon.defaultProps.style, props.style);

  return graphics[graphic].call(this, style);
}

Icon.defaultProps = {
  style: {
    display: 'inline-block',
    fill: 'black',
    stroke: 'none',
    strokeWidth: 0
  }
};

Icon.propTypes = {
  children: _react.PropTypes.node,
  graphic: _react.PropTypes.string,
  style: _react.PropTypes.shape({
    fill: _react.PropTypes.string,
    stroke: _react.PropTypes.string,
    strokeWidth: _react.PropTypes.oneOfType([_react.PropTypes.string, _react.PropTypes.number])
  })
};